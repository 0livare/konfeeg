{
  "name": "@demo/vite",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "types": "tsc",
    "format": "prettier --write .",
    "demo": "pnpm run build && pnpm run preview"
  },
  "dependencies": {
    "konfeeg": "workspace:*"
  },
  "devDependencies": {
    "prettier": "catalog:default",
    "typescript": "~6.0.2",
    "vite": "^8.0.12"
  }
}
