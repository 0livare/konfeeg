{
  "name": "konfeeg",
  "version": "0.0.10",
  "description": "Build a validated, strongly-typed config object, and use it everywhere.",
  "author": "Zach Olivare <https://github.com/0livare>",
  "license": "ISC",
  "repository": "https://github.com/0livare/konfeeg",
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.mts",
  "exports": {
    ".": {
      "import": {
        "types": "./dist/index.d.mts",
        "default": "./dist/index.mjs"
      },
      "require": {
        "types": "./dist/index.d.cts",
        "default": "./dist/index.cjs"
      }
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "tsdown --watch",
    "build": "tsdown",
    "test": "vitest --run",
    "pub": "pnpm run build && npx bumpp && pnpm publish"
  },
  "nx": {
    "targets": {
      "types": {
        "executor": "nx:run-commands",
        "options": {
          "commands": [
            "tsc",
            "node ./node_modules/typescript5/bin/tsc"
          ],
          "cwd": "{projectRoot}",
          "parallel": false
        },
        "dependsOn": [
          "^build"
        ]
      },
      "format": {},
      "lint": {}
    }
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:default",
    "tsdown": "^0.22.2",
    "typescript": "catalog:default",
    "typescript5": "npm:typescript@5.9.3",
    "vitest": "^4.1.8"
  },
  "keywords": [
    "environment",
    "config",
    "configuration",
    "typescript",
    "type-safe",
    "validation",
    "browser",
    "node"
  ]
}
